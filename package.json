{
  "name": "plugin-auto-redirect",
  "version": "1.0.0",
  "description": "A Docusaurus plugin for auto redirecting an old URL to the updated one",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {
    "docusaurus-plugin-dotenv": "^1.0.1",
    "dotenv": "^16.0.1",
    "@aws-sdk/client-s3": "^3.190.0",
    "@docusaurus/plugin-client-redirects": "^2.1.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@docusaurus/types": "^2.1.0",
    "@types/lodash": "^4.14.186",
    "typescript": "^4.8.4"
  }
}
